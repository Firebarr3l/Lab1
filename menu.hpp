#ifndef MENU_HPP
#define MENU_HPP

#include <vector>
#include "rwFunc.hpp"

// Прототипы функций
void printData(const std::vector<GardenAssociation>& data);
void aboutProgram();
void menu(std::vector<GardenAssociation>& data);

#endif // MENU_HPP
